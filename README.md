```bash
git clone https://github.com/mib1213/LudoSimulation.git
git add --all
git branch "branch_name"
git switch "branch_name"
git branch # to check the current branch (green color)
git add --all
```